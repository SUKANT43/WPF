<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FlipCard.Controls"
    xmlns:helper="clr-namespace:FlipCard.Helper">

    <Style TargetType="{x:Type local:ProductDisplayCard}">
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ProductDisplayCard}">

                    <Grid x:Name="RootGrid" RenderTransformOrigin="0.5,0.5">

                        <Grid.RenderTransform>
                            <ScaleTransform x:Name="FlipScale" ScaleX="1"/>
                        </Grid.RenderTransform>

                        <!-- FRONT SIDE -->
                        <Border x:Name="FrontCard"
                                Background="#fefefe"
                                CornerRadius="20"
                                Padding="16">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="3*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- IMAGE SECTION -->
                                <Grid Grid.Row="0">
                                    <Border CornerRadius="5">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#e4e4f0" Offset="0"/>
                                                <GradientStop Color="#99a7c2" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>

                                        <Image Source="{Binding ImageSource, RelativeSource={RelativeSource TemplatedParent}}"
                                               Width="200"
                                               Height="180"/>
                                    </Border>

                                    <!-- Discount Badge -->
                                    <Border Background="#df4c4f"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Top"
                                            Margin="2"
                                            CornerRadius="8"
                                            Panel.ZIndex="3"
                                            Visibility="{Binding IsDiscountPercentageVisible,
                                                         RelativeSource={RelativeSource AncestorType=Control}}">
                                        <TextBlock Text="{Binding DiscountPercentage,
                                                          RelativeSource={RelativeSource TemplatedParent},
                                                          StringFormat={}{0}% OFF}"
                                                   Foreground="White"
                                                   FontWeight="Bold"
                                                   FontSize="16"
                                                   Padding="6 3"/>
                                    </Border>

                                    <Button
                                        VerticalAlignment="Top"
                                        HorizontalAlignment="Right"
                                        Margin="4"
                                        Background="Transparent"
                                        x:Name="likeButton"
                                        >
                                        <StackPanel>
                                            <Image Source="{Binding HeartPngSource,
                                                         RelativeSource={RelativeSource TemplatedParent}}"
                                                   Width="30"/>
                                        </StackPanel>
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border BorderBrush="Transparent"
                                                        BorderThickness="0"
                                                        Background="{TemplateBinding Background}">
                                                    <ContentPresenter
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"/>

                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    
                                </Grid>

                                <!-- NAME + RATING -->
                                <StackPanel Grid.Row="1">

                                    <TextBlock
                                        Foreground="#03081d"
                                        Text="{Binding ProductName,
                                               RelativeSource={RelativeSource AncestorType=Control}}"
                                        FontWeight="Bold"
                                        TextWrapping="WrapWithOverflow"
                                        FontSize="20"/>

                                    <StackPanel Orientation="Horizontal" Margin="0 10 0 0">

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="★" Foreground="Gold" FontSize="18"/>
                                            <TextBlock Text="★" Foreground="Gold" FontSize="18"/>
                                            <TextBlock Text="★" Foreground="Gold" FontSize="18"/>
                                            <TextBlock Text="★" Foreground="Gold" FontSize="18"/>
                                            <TextBlock Text="★" Foreground="Gold" FontSize="18"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                                            <TextBlock Text="(" Foreground="#7b7c80" FontSize="13"/>
                                            <TextBlock Text="{Binding RatingCount,
                                                              RelativeSource={RelativeSource AncestorType=Control}}"
                                                       FontSize="13"
                                                       Foreground="#7b7c80"/>
                                            <TextBlock Text=")" FontSize="13" Foreground="#7b7c80"/>
                                        </StackPanel>

                                    </StackPanel>
                                </StackPanel>

                                <!-- PRICE SECTION -->
                                <StackPanel Grid.Row="2">

                                    <StackPanel Orientation="Horizontal">

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="₹"
                                                       FontWeight="Bold"
                                                       FontSize="18"
                                                       helper:HighLightHelper.IsHighlighted="{Binding IsPriceIncreased,
                                                                                             RelativeSource={RelativeSource TemplatedParent}}"/>

                                            <TextBlock Text="{Binding OfferedPrice,
                                                              RelativeSource={RelativeSource TemplatedParent}}"
                                                       FontWeight="Bold"
                                                       FontSize="18"
                                                       helper:HighLightHelper.IsHighlighted="{Binding IsPriceIncreased,
                                                                                             RelativeSource={RelativeSource TemplatedParent}}"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="7 4 0 0">
                                            <TextBlock Text="₹"
                                                       Foreground="#7b7c80"
                                                       FontSize="15">
                                                <TextBlock.TextDecorations>
                                                    <TextDecorationCollection>
                                                        <TextDecoration Location="Strikethrough"/>
                                                    </TextDecorationCollection>
                                                </TextBlock.TextDecorations>
                                            </TextBlock>

                                            <TextBlock Text="{Binding OriginalPrice,
                                                              RelativeSource={RelativeSource AncestorType=Control}}"
                                                       Foreground="#7b7c80"
                                                       FontSize="15">
                                                <TextBlock.TextDecorations>
                                                    <TextDecorationCollection>
                                                        <TextDecoration Location="Strikethrough"/>
                                                    </TextDecorationCollection>
                                                </TextBlock.TextDecorations>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal"
                                                    Margin="10 5 0 0"
                                                    Visibility="{Binding IsDiscountPercentageVisible,
                                                                 RelativeSource={RelativeSource AncestorType=Control}}">
                                            <TextBlock Text="{Binding DiscountPercentage,
                                                              RelativeSource={RelativeSource TemplatedParent}}"
                                                       Foreground="#004f2f"
                                                       FontSize="14"/>
                                            <TextBlock Text="%"
                                                       Foreground="#004f2f"
                                                       FontSize="14"/>
                                            <TextBlock Text="off"
                                                       Margin="5 0 0 0"
                                                       Foreground="#004f2f"
                                                       FontSize="14"/>
                                        </StackPanel>

                                    </StackPanel>
                                </StackPanel>

                                <!-- STOCK -->
                                <StackPanel Grid.Row="3"
                                            Orientation="Horizontal"
                                            Margin="0 6 0 0">

                                    <Border helper:StockHelper.IsOutOfStock="{Binding IsOutOfStock,
                                                                              RelativeSource={RelativeSource AncestorType=Control}}"
                                            CornerRadius="100"
                                            Width="8"
                                            Height="8"/>

                                    <TextBlock helper:StockHelper.IsOutOfStock="{Binding IsOutOfStock,
                                                                                RelativeSource={RelativeSource AncestorType=Control}}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Margin="4 0 0 0"/>
                                </StackPanel>

                                <!-- BUTTON -->
                                <Button Content="Add to Cart"
                                        Grid.Row="4"
                                        Margin="0 5 0 0"
                                        Foreground="White"
                                        Background="#1b61dd"
                                        FontWeight="Bold"
                                        Padding="7"
                                        helper:StockHelper.IsOutOfStock="{Binding IsOutOfStock,
                                                                          RelativeSource={RelativeSource AncestorType=Control}}">

                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="8"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>

                                </Button>

                            </Grid>
                        </Border>

                        <!-- BACK SIDE -->
                        <Border x:Name="BackCard"
                                Background="#97a7c1"
                                CornerRadius="20"
                                Padding="16"
                                Visibility="Visible">

                            <Border Background="#fdfdff"
                                    Padding="20"
                                    CornerRadius="20">

                                <Grid>

                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="2*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0">
                                        <TextBlock Text="Product Details"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   TextWrapping="Wrap"/>

                                        <Border Height="2"
                                                Margin="0,10,0,0"
                                                Background="#e0e1ea"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Margin="0,15,0,0">
                                        <TextBlock Text="{Binding ProductDescription,
                                                          RelativeSource={RelativeSource TemplatedParent}}"
                                                   FontSize="14"
                                                   Foreground="#444"
                                                   TextWrapping="Wrap"/>

                                        <Border Height="2"
                                                Margin="0,10,0,0"
                                                Background="#e0e1ea"/>
                                    </StackPanel>

                                    <ItemsControl Grid.Row="2"
                                                  Margin="0,15,0,0"
                                                  ItemsSource="{Binding ProductFeatures,
                                                                RelativeSource={RelativeSource TemplatedParent}}">

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">

                                                    <Image Source="pack://application:,,,/FlipCard;component/Image/tick.png"
                                                           Width="14"
                                                           Height="14"
                                                           Margin="0,0,6,0"/>

                                                    <TextBlock Text="{Binding}"
                                                               FontSize="14"
                                                               VerticalAlignment="Center"/>

                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>

                                    </ItemsControl>

                                    <Grid Grid.Row="3" Margin="0,20,0,0">
                                        <Button Content="Buy Now"
                                                Width="150"
                                                Height="45"
                                                HorizontalAlignment="Left"
                                                Background="#fc7523"
                                                Foreground="White"
                                                FontWeight="Bold"
                                                Cursor="Hand">

                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border CornerRadius="25"
                                                            Background="{TemplateBinding Background}">
                                                        <ContentPresenter
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>

                                        </Button>
                                    </Grid>

                                </Grid>

                            </Border>
                        </Border>

                    </Grid>

                    <!-- TRIGGERS -->
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsFlipped" Value="True">
                            <Setter TargetName="FrontCard" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="BackCard" Property="Visibility" Value="Visible"/>

                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FlipScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         From="1"
                                                         To="0"
                                                         Duration="0:0:0.15"
                                                         AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>

                        <Trigger Property="IsFlipped" Value="False">
                            <Setter TargetName="FrontCard" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="BackCard" Property="Visibility" Value="Collapsed"/>

                            <!--<Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FlipScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         From="1"
                                                         To="0"
                                                         Duration="0:0:0.15"
                                                         AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>-->
                        </Trigger>

                    </ControlTemplate.Triggers>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>