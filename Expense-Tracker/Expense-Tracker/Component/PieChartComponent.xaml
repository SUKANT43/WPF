<UserControl x:Class="Expense_Tracker.Component.PieChartComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Expense_Tracker.Component"
             mc:Ignorable="d" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <lvc:PieChart LegendLocation="Right">
            <lvc:PieChart.Series>
                <lvc:PieSeries Title="Food"
                       Values="{Binding FoodValue}"
                       DataLabels="True"/>

                <lvc:PieSeries Title="Transport"
                       Values="{Binding TransportValue}"
                       DataLabels="True"/>

                <lvc:PieSeries Title="Shopping"
                       Values="{Binding ShoppingValue}"
                       DataLabels="True"/>
            </lvc:PieChart.Series>
        </lvc:PieChart>
    </Grid>
</UserControl>
