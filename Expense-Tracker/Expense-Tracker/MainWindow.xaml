<Window x:Class="Expense_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:component="clr-namespace:Expense_Tracker.Component"
        mc:Ignorable="d"
        Title="Expense Tracker"
        Height="450"
        Width="800"
        Background="{DynamicResource AppBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TopBar -->
        <component:TopBarComponent
            Grid.Row="0"
            UserName="{Binding Name}"
            ProfileLetter="{Binding ProfileLetter}"
            NavigateToHomeCommand="{Binding NavigateToHomeCommand}"
            NavigateToReportCommand="{Binding NavigateToReportCommand}"
            Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}"/>
        <!-- Page Content -->
        <ContentControl Grid.Row="1"
                        Content="{Binding CurrentView}" />

    </Grid>
</Window>