<UserControl x:Class="Inventory.Controller.InputCardController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory.Controller"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                       Grid.ColumnSpan="2"
                       Text="Create Item"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,20"
                               HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="Product Name:"
                       VerticalAlignment="Center"
                       Margin="0,5"/>
        <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Text="{Binding ProductName,UpdateSourceTrigger=PropertyChanged}"
                     Height="30"
                     Margin="0,5"/>

        <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="Original Price:"
                       VerticalAlignment="Center"
                       Margin="0,5"/>
        <TextBox Grid.Row="2"
                     Grid.Column="1"
                        Text="{Binding OriginalPrice, UpdateSourceTrigger=PropertyChanged}"
                     Height="30"
                     Margin="0,5"/>

        <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Text="Offered Price:"
                       VerticalAlignment="Center"
                       Margin="0,5"/>
        <TextBox Grid.Row="3"
                    Text="{Binding OfferedPrice, UpdateSourceTrigger=PropertyChanged}"
                     Grid.Column="1"
                     Height="30"
                     Margin="0,5"/>

        <TextBlock Grid.Row="4"
                       Grid.Column="0"
                       Text="GST %:"
                       VerticalAlignment="Center"
                       Margin="0,5"/>
        <TextBox Grid.Row="4"
                    Text="{Binding GSTpercentage, UpdateSourceTrigger=PropertyChanged}"
                     Grid.Column="1"
                     Height="30"
                     Margin="0,5"/>

        <Button Grid.Row="5"
        Grid.ColumnSpan="2"
        Content="Save Item"
        Height="35"
        Margin="0,20,0,0"
        Foreground="White"
        FontWeight="Bold"
        BorderThickness="0"
        Cursor="Hand"
         Command="{Binding SaveCommand}">

            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource PrimaryAccentColor}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background"
                                        Value="{DynamicResource HoverAccentColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
