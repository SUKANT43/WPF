<Page x:Class="Inventory.View.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory.View"
             xmlns:controller="clr-namespace:Inventory.Controller"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition  Width="10*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <!--top panel-->
        <StackPanel Grid.ColumnSpan="2" VerticalAlignment="Center">
            <controller:TopBarControl />
        </StackPanel>

        <!-- input panel -->
        <StackPanel Grid.Row="1" Grid.RowSpan="2" VerticalAlignment="Top">
            <Border Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="20"
            Width="350">
                <controller:InputCardController x:Name="InputCard"/>
            </Border>
        </StackPanel>

        <DataGrid Grid.Row="2"
          Grid.Column="1"
          Margin="10"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          HeadersVisibility="Column"
          Background="{DynamicResource CardBackground}"
          ItemsSource="{Binding Products}"
          BorderBrush="{DynamicResource BorderColor}"
          IsReadOnly="True"
          BorderThickness="1"
          SelectedItem="{Binding SelectedItem,Mode=TwoWay}"
                  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"
                            Binding="{Binding Id}"
                            Width="*" />

                <DataGridTextColumn Header="Product Name"
                            Binding="{Binding ProductName}"
                            Width="2*" />

                <DataGridTextColumn Header="Original Price"
                            Binding="{Binding OriginalPrice}"
                            Width="*" />

                <DataGridTextColumn Header="Offered Price"
                            Binding="{Binding OfferedPrice}"
                            Width="*" />

                <DataGridTextColumn Header="GST %"
                            Binding="{Binding GSTpercentage}"
                            Width="*" />
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Page>
